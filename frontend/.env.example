# Gateway (Traefik) — required for microservices routes like GET /companies/public.
# Start infra first: yarn dev:infra (from repo root), then yarn dev.
VITE_API_URL=http://localhost:8080

# Legacy monolith only (yarn dev:legacy from repo root, no gateway needed):
# VITE_API_URL=http://localhost:4000
