# Gateway (Traefik) — start infra first: yarn dev:infra, then a dev bundle.
VITE_API_URL=http://localhost:8080
# Student RabbitMQ lab page at /student/rabbitmq (requires yarn dev:rabbitmq-lab)
VITE_ENABLE_RABBITMQ_LAB=true
