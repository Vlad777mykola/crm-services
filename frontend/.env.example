VITE_API_URL=http://localhost:4000

# Once the gateway is running (docker/docker-compose.microservices-core.yml, see
# docs/architecture/url-convention.md), point this at the gateway instead - every
# path stays identical, only the host/port changes:
# VITE_API_URL=http://localhost:8080
