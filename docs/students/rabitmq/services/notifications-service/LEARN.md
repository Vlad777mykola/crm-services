# notifications-service — Student Guide

**CURRENT VERIFIED**

Pure consumer — reacts to facts, writes `email_logs` (simulated email). Example B downstream leg.

Handlers: `handlers/domain-events.ts`, `handlers/analytics-events.ts`.

**Exercise:** Read `consumer.test.ts` for duplicate handling.
