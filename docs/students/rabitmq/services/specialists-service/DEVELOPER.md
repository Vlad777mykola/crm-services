# specialists-service — Developer Guide

Outbox only: `src/outbox/outbox-repository.ts`. No consumer to add unless service scope changes.

Publisher: separate `outbox-publisher` with `OUTBOX_SCHEMA=specialists_schema`.
