# Disposable overrides for full docker/prod/compose.yml stack (future full-matrix smoke).
# Minimal prod-parity smoke uses docker/smoke/compose.yml instead.

services:
  postgres:
    ports:
      - '35432:5432'
    volumes: []

  gateway:
    ports:
      - '38080:80'
