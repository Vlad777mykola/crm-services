{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://crm-services/contracts/events/company.updated.v1.json",
  "title": "company.updated",
  "description": "Published when a company is updated via PATCH /companies/:companyId (profile fields and/or status). Exchange: domain.events. Routing key: company.updated.",
  "type": "object",
  "required": ["companyId", "name", "status"],
  "additionalProperties": false,
  "properties": {
    "companyId": { "type": "string", "format": "uuid" },
    "name": { "type": "string" },
    "status": { "type": "string", "enum": ["draft", "published", "suspended"] }
  }
}
