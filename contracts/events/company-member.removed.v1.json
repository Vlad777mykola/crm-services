{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://crm-services/contracts/events/company-member.removed.v1.json",
  "title": "company-member.removed",
  "description": "Published when a member's status is set to removed (via PATCH or DELETE). Exchange: domain.events. Routing key: company-member.removed.",
  "type": "object",
  "required": ["companyId", "userId"],
  "additionalProperties": false,
  "properties": {
    "companyId": { "type": "string", "format": "uuid" },
    "userId": { "type": "string", "format": "uuid" }
  }
}
