{
  "name": "@crm/authz-kit",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Thin, DB-free authorization primitives shared across services. No TypeORM, no Express, no database access - each service injects its own lookup implementation and keeps its own domain policy.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@crm/http-kit": "0.0.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "5.9.3"
  }
}
